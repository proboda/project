{
  "name": "login-app-server",
  "version": "1.0.0",
  "main": "complete-server.js",
  "scripts": {
    "start": "node complete-server.js",
    "dev": "nodemon server/server.js",
    "client": "cd login-app && npm start",
    "dev:full": "concurrently \"npm run dev\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.9"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "pg": "^8.14.1"
  }
}
